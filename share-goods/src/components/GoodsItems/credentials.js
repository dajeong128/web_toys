module.exports = {
    databaseURL: "https://goods-204a7.firebaseio.com/goods.json",
    credential: {
      // JSON data from firebase-key.json
      // service account key downloaded from firebase
    }
  };